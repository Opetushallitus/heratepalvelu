(ns oph.heratepalvelu.tep.tepCommon
  (:require [oph.heratepalvelu.common :as c]))

(defn get-new-loppupvm [nippu]
  ;; TODO compute new loppupvm if message hasn't been sent, otherwise return nil
  )
